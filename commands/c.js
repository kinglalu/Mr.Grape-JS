
const myStructure = {
  first: "blah",
  second: "foo",
  changeme: "initial",
  isCool: false
  sub: {
    yay: true,
    thing: "amagig"
  }
}


async function setdis() {
item.set("item", myStructure);
}

setdis();

async function try() {

message.channel.send(`${item.get("item,"sub.thing")}`);
}

try();
